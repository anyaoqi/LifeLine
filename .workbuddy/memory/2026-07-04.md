## 2026-07-04

### 项目启动
- 启动 Life-point 前端项目（Vue 3 + Vite 8 + TypeScript + Tailwind CSS）
- Vite 开发服务器运行在 http://localhost:5173/
- 使用 Node.js 22.22.2 (managed)，PowerShell 直接调用 vite.js 绕过 .bin/.cmd 兼容问题

### Phase 3 统计面板开发（v0.3.0 阶段一）
- 阅读 docs/开发规划.md 与 设计理念与UI指南.md，确认 Phase 2 已完成，进入 Phase 3
- 新增 `src/utils/stats.ts`：纯函数统计工具（年度/分类/类型/月份/总览 + 热力色阶），无副作用便于后续单测
- 新增 `src/components/Stats/` 下 4 个纯 CSS+Vue 图表组件，未引入图表库：
  - StatsSummaryCards.vue（6 张总览卡片）
  - StatsBarChart.vue（年度趋势柱状图，自动补全空年份，超 20 年横向滚动）
  - StatsCategoryChart.vue（分类分布水平条形图，带配色+emoji+百分比）
  - StatsMonthHeatmap.vue（12 月份热力图，CSS 变量切换浅/深色 5 级色阶）
- 新增 `src/views/StatsView.vue`，组合所有图表 + 空数据/加载态处理
- 路由：注册 `/stats`（requiresUser 守卫）；导航：AppHeader 加「统计」入口；HomeView 加快速入口
- 验证：vue-tsc --noEmit 零错误；vite build 通过（StatsView chunk 10.14 kB）
- 更新 docs/开发规划.md 进度：Phase 3 标记为开发中，统计面板已完成

### 导出时间线为图片（v0.3.0 阶段二）
- 复核统计面板代码：与 eventStore/userStore/types API 全部对齐，类型检查零错误
- 新增依赖 `html-to-image@^1.11.13`（~6KB gzipped，基于 SVG foreignObject，比 html2canvas 更准）
- 在 `src/services/exportService.ts` 新增图片导出能力：
  - `exportNodeToImage(node, options)` —— 封装 toPng/toJpeg，自动按 `<html>.dark` 选背景色
  - `buildExportFilename(user, prefix)` —— 生成带用户名的安全文件名
- 改造 `TimelinePageView.vue`：
  - 用 `ref="exportArea"` 包裹页头+筛选+时间线（排除浮动 + 按钮）
  - 页头右侧加「📷 导出图片」ghost 按钮（有事件时才显示）
  - 导出中 loading 态 + 失败 3 秒自动消失的错误提示条
  - 补齐之前缺失的 AppButton 显式 import
- package.json version 升级 0.2.0 → 0.3.0
- 验证：vue-tsc --noEmit 零错误；vite build 通过（exportService chunk 14.62kB，TimelinePageView 16.76kB）
- 更新 docs/开发规划.md：导出图片标记完成，待办移除该项
